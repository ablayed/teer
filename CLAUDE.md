# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) **and Codex CLI** when working with code in this repository. It is the **shared source of truth** for both agents — the solo developer alternates between them depending on token budget, so this file guarantees one agent resumes exactly where the other left off. **Read it fully before changing anything. Update the "Phase tracker" section at the end of every phase.**

> `AGENTS.md` mirrors this file (symlink or one-line pointer). If they ever diverge, this file wins.

## Project

Tëër (Wolof: "to receive / welcome") is a **mobile-first, French-only PWA** (Next.js 15 App Router, React 19) for **cash-on-delivery (COD) operations** of Senegalese Shopify merchants. It is a **COD operations cockpit, not an ERP and not a Shopify clone**. Backend is Supabase (Postgres + Auth + RLS). Requires Node 22, pnpm, and the Supabase CLI. In production at `teer-dev.vercel.app`.

**The pain point:** Shopify wasn't designed for African commerce. Merchants fall back to Excel for stock, driver cash, cancellations, and margin. Shopify doesn't track the COD operational cycle (confirmation calls, attempts, driver assignment, cash remittance, returns). Tëër owns that loop: call → confirm → schedule → assign to a driver → deliver → collect cash → reconcile the till → track stock and real margin — all in one place.

**Market:** Senegalese Shopify merchants who also sell via WhatsApp/TikTok/Facebook; cash delivery; **successful-delivery rate ~25-30%** (high cancellation/refusal — structural, and it drives several design decisions, see "Locked decisions"). Persona: importer-reseller, 5-50 orders/day with peaks at 120 (Tabaski, Black Friday), 2 part-time tele-operators + moto drivers.

**The moat:** the full COD operational loop — Shopify OAuth multi-shop + call workflow + COD state machine + driver cash reconciliation + per-customer/per-product reliability + stock & landed cost for import-resale. No Francophone West African competitor combines all of it. Competitors own the storefront (YouCan, Storeino, EasyAfrik), the trucks (Logidoo, Paps), or the ledger (STOCKALIO) — Tëër owns the operational loop.

## Commands

```bash
pnpm dev          # next dev
pnpm build        # next build
pnpm lint         # biome check . (lint + format check)
pnpm format       # biome format --write .
pnpm typecheck    # tsc --noEmit
pnpm test:unit    # vitest run tests/unit
pnpm test:rls     # vitest run tests/rls (needs a running Supabase, see below)
pnpm test:e2e     # playwright test (auto-starts `pnpm dev` as webServer)
pnpm db:types     # regenerate lib/supabase/database.types.ts from the linked project
```

Run a single unit test: `pnpm vitest run tests/unit/orders/<file>.test.ts` (or `pnpm vitest -t "<test name>"`).
Run a single e2e spec/project: `pnpm exec playwright test tests/e2e/orders-transitions.spec.ts --project=chromium`.

CI (`.github/workflows/ci.yml`) runs lint → typecheck/test-unit/test-rls → test-e2e. `test:rls` spins up a local stack via `supabase start` and reads keys from `supabase status`; locally it needs `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (loaded via `.env.test`; RLS tests `skipIf` the service role key is absent). **Ports 54321-54324 are shared with another local project — stop it before starting Tëër's stack.**

DB setup: `supabase link --project-ref <ref>` → `supabase db push`. Migrations live in `supabase/migrations/` and are applied in order; there is no migration ORM. **Latest applied migration: `0060` (in prod, types regenerated).**

## Engineering rules (non-negotiable)

1. **Invent NOTHING.** No table, column, function, action, route, or component that doesn't exist exactly in the repo. If a referenced name is missing → **STOP** and flag it. (This guardrail already prevented building the stock module on a `product` table that didn't exist yet.)
2. **Migration → STOP.** Write the `.sql` file and stop. The developer runs `pnpm exec supabase db push` then `pnpm db:types` locally, confirms, then implementation continues. You never run `db push` yourself.
3. **Schema deploys to prod BEFORE code.** No TS line references a table/column until its migration is confirmed applied in prod. (Pushing code before schema caused a production error screen in Phase 2.)
4. **One commit per deliverable**, French message prefixed by phase (e.g. `phase3:`). No opportunistic refactors bundled in.
5. **Sanity loop before every code commit:** `pnpm typecheck && pnpm lint && pnpm test:unit && pnpm build`, plus `pnpm test:rls` **non-skipped** (RLS tests prove tenant isolation; never ignored). Never commit red.
6. **`performTransition` is the ONLY write gate for order state.** No applicative `.from('orders').update(...)` on state. The client never decides a transition; it only renders the `allowedActions` the server returns.
7. **Stock is a SIDE EFFECT, never a precondition.** An order line that can't be resolved to a product never fails a transition — skip the stock movement for that line.
8. **Schema/RBAC discipline:** RLS FORCE + deny-by-default; separate policies per operation; every `UPDATE` policy has `WITH CHECK`; new columns start nullable until a phase explicitly requires `NOT NULL` (a transition must never be silently rejected by a constraint). `unit_cost`/margin are hidden from `agent` at the column level.
9. **Never switch agents on a dirty tree.** Commit + push before changing agent; `git status` must be empty (otherwise the other agent doesn't see in-progress work and may overwrite it).

## Architecture

**Mutations go through `next-safe-action`, never raw route handlers.** `lib/actions/safe-action.ts` defines the action clients:
- `actionClient` — base, requires `{ actionName, section }` metadata.
- `authActionClient` — adds `ctx.user` + `ctx.supabase` (throws `UNAUTHENTICATED`).
- `requireRole(...roles)` — adds `ctx.member` (`{ id, merchantAccountId, role }`) by looking up `merchant_member`; throws `FORBIDDEN`.

Server errors are flattened to opaque strings (`UNEXPECTED_ERROR`, `UNAUTHENTICATED`, `FORBIDDEN`). Each file in `lib/actions/` exposes one domain's server actions (`orders`, `customers`, `finance`, `team`, `shops`, `shopify`, `products`, `auth`, …).

**The COD order lifecycle is the core domain — now a four-dimension model.** Since migration `0021`, an order is NOT a single status. It has **four orthogonal dimensions** stored in separate columns on `orders`:

| Dimension | Column | Values |
|---|---|---|
| Lifecycle | `order_state` | `open · completed · cancelled · returned` |
| Calls | `call_state` | `to_call · callback · validated · unreachable` |
| Delivery | `delivery_state` | `unassigned · scheduled · assigned · out_for_delivery · delivered · failed · returned` |
| Cash | `cash_state` | `not_due · expected · collected · remitted · discrepancy` |

Support columns: `attempt_count`, `next_contact_at`, `scheduled_for`, `cancel_reason`, `assigned_driver_id`.

**`cod_status` (legacy 8 values: `A_APPELER · TENTEE · CONFIRMEE · PROGRAMMEE · EN_LIVRAISON · LIVREE · REFUSEE · ANNULEE`) is NOT the source of truth and NOT a generated column.** It is a real column kept in sync by a **`BEFORE INSERT OR UPDATE` dual-write trigger** (`derive_legacy_cod_status`, migration `0023`, `SECURITY DEFINER`). You write the **four dimensions**; the trigger derives and writes `cod_status` in the same operation. **Never write `cod_status` directly.** Derivation priority (top→bottom): `delivery_state ∈ {failed,returned}` or `order_state=returned` → `REFUSEE` · `order_state=cancelled` → `ANNULEE` · `delivery_state=delivered` → `LIVREE` · `delivery_state ∈ {out_for_delivery,assigned}` → `EN_LIVRAISON` · `delivery_state=scheduled` → `PROGRAMMEE` · `call_state=validated` → `CONFIRMEE` · `call_state=callback` → `TENTEE` · else → `A_APPELER`. `cod_status` is not yet dropped (transition window); `reconcile_order_cod_status()` (`0024`) proves zero drift dimensions↔legacy.

The lifecycle is defined in layers that must stay in sync:
1. `lib/domain/order-state-machine.ts` — legal transitions, `canTransition`/`assertTransition`.
2. `lib/domain/order-transition-actions.ts` — maps user *actions* (`confirmer`, `livrer`, …) to dimension changes via `transitionCatalog`, and which `TeamRole` may perform each action.
3. `lib/actions/transitions.ts` — `performTransitionForContext` orchestrates: role check → load order → `canTransition` guard → call the `transition_order` Postgres RPC (which writes the dimensions atomically, the trigger derives `cod_status`, **and posts stock movements via `post_stock_movement` in the same transaction** — all or nothing) → reload → write `audit_log` (service-role admin client) → `revalidatePath`, returning `{ order, allowedActions }`.

The actual state write happens in the **`transition_order` Postgres function** (migrations `0008`, `0016`, `0020`, extended in Phase 1 to write dimensions), not in TS — the TS layer guards and audits, Postgres enforces. When changing the lifecycle, update the TS state machine, the catalog, the RPC, and the RLS policies together.

**Roles and permissions.** Three roles: `owner`, `manager`, `agent` (`lib/team/permissions.ts`). Capabilities are checked in TS (`hasCapability`, `canChangeMemberRole`, …) AND enforced in Postgres RLS, keyed on `merchant_account_id` via `current_member_role(...)`; **RLS FORCE on all tenant tables**, and `tests/rls/` proves tenant isolation.

> **Corrected (was stale):** agents are **no longer** given a status-scoped row view. Migration `0020` (Phase 0) **removed the status filter from `orders_select`** — it was the cause of a white-screen bug (a confirmed order disappeared from the agent's view mid-workflow). The agent now **sees all of the tenant's orders**; the per-view filtering is **application-level** (saved views), not RLS. What RLS still restricts is the agent's **write scope**: `orders_update`'s `WITH CHECK` limits an `agent` to producing `cod_status ∈ {TENTEE, CONFIRMEE, PROGRAMMEE, EN_LIVRAISON}` (owner/manager: all). Because `cod_status` is trigger-derived, the post-trigger row must satisfy that `WITH CHECK` — verified by `tests/rls/orders-dimensions.rls.test.ts`.

**Two Supabase clients.** `lib/supabase/server.ts` (`createSupabaseServerClient`, cookie-based, respects RLS — use this in actions/RSC) vs. the **service-role admin client** created inline in `transitions.ts` (bypasses RLS — only for audit writes and trusted server work). `lib/supabase/client.ts` is the browser client.

**Env is validated with Zod** in `lib/env.ts`: `publicEnv` (NEXT_PUBLIC_*) and `env` (adds server secrets like `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, Shopify keys). Import from here, not `process.env` directly.

**Routing.** `app/(app)/` is the authenticated shell (commandes, clients, produits, finances, tableau, boutiques, parametres); `app/(marketing)/` is public. `app/api/` holds non-action route handlers: `shopify/` (OAuth install/callback + HMAC-verified webhooks), `rapport/` (PDF generation), `cron/keep-alive`. Shopify integration lives in `lib/shopify/` (oauth, webhook-verify, orders/shop/product sync, token crypto).

**Shopify multi-app (registre `client_id → credentials`).** Tëër héberge DEUX apps Shopify sur un seul déploiement (`teer-dev.vercel.app`) : **Teer Dev** (publique, par défaut, `SHOPIFY_API_KEY/SECRET`) et **Teer Pilote** (custom, `SHOPIFY_PILOTE_API_KEY/SECRET`). Le routage se fait sur le `client_id`. Cœur pur testable : `lib/shopify/app-registry.ts` (`createShopifyAppRegistry` → `getByClientId/getDefault/all`, importable sans `lib/env`) ; singleton lié aux env : `lib/shopify/apps.ts` (`getShopifyAppByClientId`, `getDefaultShopifyApp`, `getShopifyAppForShop`). Une app aux credentials incomplets n'est pas enregistrée (warning au boot, pas de crash). **Base Supabase PARTAGÉE** : un marchand est un tenant quelle que soit l'app d'installation. **Chaque boutique mémorise `shop.shopify_client_id`** (migration `0048`, nullable, backfill = Teer Dev) → sert à router le secret HMAC des webhooks ET les credentials sortants (refresh token / bulk / réconciliation) **par app**. Install : `/api/shopify/install` lit un `client_id` query (sinon défaut) → 400 si inconnu → le `client_id` voyage dans le **state OAuth signé** (signature interne inchangée) → relu au callback pour choisir le secret (vérif HMAC + échange). Webhooks : lookup boutique par `x-shopify-shop-domain` → secret de son app ; boutique absente de la base (conformité GDPR, désinstallée, jamais installée) → `verifyWebhookHmacAnySecret` essaie **tous** les secrets enregistrés (timing-safe, sans court-circuit) pour rester vérifié (401 sur HMAC invalide) sur les DEUX apps. **Non-régression Teer Dev garantie** : un `client_id` null/inconnu retombe sur l'app par défaut.

**i18n.** Single locale `fr`, no URL prefix (`i18n/request.ts`), via `next-intl`. All UI strings centralized in `messages/fr.json`, consumed with `useTranslations`/`getTranslations` — **do not hardcode UI text**. Wolof planned (cookie-switched). `html lang="fr-SN"`; dates/money formatted for Senegal (FCFA, `Africa/Dakar`) — see `lib/format/`. Phone normalization in `lib/address/phone-sn.ts` (`normalizeSenegalPhone`).

## Data model — canonical entities (exact names)

Tenant tables (all carry `merchant_account_id`, RLS FORCE): `merchant_account` · `merchant_member` (role `owner|manager|agent`) · `shop` · `webhook_event` · `customer` · `product` (catalogue, Phase 3a/`0027`) · `orders` · `order_state_transition` · `order_line` (Phase 3b/`0028`) · `call_log` · `audit_log` (extended: `prior_state, next_state, source, reason`) · `invitation` · `driver` (non-auth) · `cash_settlement` · `settlement_allocation` · `settlement_shortfall` · `merchant_settings` · `stock_movement` (append-only, Phase 3b/`0028`) · `product_stock` (denormalized projection, Phase 3b/`0028`).

Key functions/RPC: `transition_order` (writes dimensions + derives `cod_status` via trigger + posts stock movements atomically via `post_stock_movement`) · `derive_legacy_cod_status` (trigger) · `current_member_role(merchant_account_id)` (SECURITY DEFINER) · `post_stock_movement(...)` (SECURITY DEFINER — ledger insert + `FOR UPDATE` + position update in one transaction) · `reconcile_order_cod_status()` · `reconcile_product_stock()` · `rebuild_product_stock()`.

## Locked decisions (do not re-litigate without explicit reason)

- **Stock — decrement at DISPATCH, not at validation.** The reserve at validation is **soft and non-blocking** (`qty_reserved`; never touches `qty_on_hand`; never blocks a new order). The **only** hard `qty_on_hand` decrement is at dispatch (assign/out_for_delivery). *Reason: ~25-30% delivery rate → hard-reserving at validation would create massive phantom reservations.*
- **RTO/return:** after dispatch the stock is physically with the courier. Cancel/fail does **not** auto-restore stock — it returns via a separate `courier_return` movement posted at physical return.
- **Stock = append-only `stock_movement` ledger + denormalized `product_stock` projection**, maintained in the same transaction (`SELECT … FOR UPDATE` + `idempotency_key`). No trigger as the primary mechanism (the engine is the sole gate). `stock_movement` is immutable: no `UPDATE`/`DELETE` policy; corrections are compensating movements. `manual_adjustment` requires a mandatory reason.
- **Cost:** moving weighted-average (CUMP), recomputed on `purchase_in`, **snapshotted** onto every outbound movement (COGS frozen at the cost in force when goods leave). SYSCOHADA/OHADA-compliant (AUDCIF Art. 44: PEPS or CMP; LIFO forbidden). *Validate the account mapping (601/6031/31) and CUMP timing with a Senegalese OHADA accountant before any tax use.*
- **Orders:** one canonical list + 8 saved views (filters, no Kanban): **Toutes · À appeler · Tentée/À rappeler · Confirmée · À livrer aujourd'hui · Cash à remettre · Annulées · Retours**. Inline status change via the engine. Manual creation uses a **product selector** (no free-text title). Search by name/phone/product.
- **Phasing:** driver stock (`driver_stock`) = Phase 4, not before. Real `unit_cost` from purchase lots = Phase 5; manual until then. Margin = Phase 6. AI assistant = Phase 8 (read-only, function-calling, never free SQL).
- **Shopify is the upstream channel, never the operational source of truth.** Call outcomes, driver assignment, stock ownership, cash remittance, cancel reasons, landed cost all live in Tëër.

## Conventions

- **All UI text is French.** Use `messages/fr.json` keys.
- Biome (not ESLint/Prettier): single quotes, semicolons, trailing commas, 100-col, 2-space. `noExplicitAny` and `noUnusedVariables` are **errors**; `useImportType` enforced (use `import type`). `console` is a warning.
- Path alias `@/*` → repo root.
- Design constraint: text/icon on the orange brand color is **always `#111`, never white**. Touch targets ≥ 44px on COD actions. Numbers in Geist Mono tabular-nums.
- `@react-pdf/renderer` is a `serverExternalPackages` entry; the report route bundles fonts from `lib/pdf/fonts/` — keep PDF code server-only.
- Sentry wraps the Next config (`next.config.mjs`) with a `/monitoring` tunnel route; PostHog analytics via `components/analytics-provider`.

## Critical gotchas

- Order status legacy column is `cod_status` (text), trigger-derived, **never written directly** — see Architecture. Order total column is `total_amount` (numeric).
- All money is stored as minor-unit bigint (FCFA, 0 decimals). Never render a raw amount — always pass through `formatMoney(amount)`. The cash field is `cash_collectable_minor` (bigint).
- **Devise : FCFA exclusivement à l'affichage (mono-devise ; multi-devise différé).** `formatMoney` **ignore volontairement** la devise stockée (`orders.currency`) et affiche toujours « F CFA » arrondi à l'entier. La colonne `orders.currency` est conservée pour un éventuel multi-devise futur (marché hors zone CFA) mais ne pilote plus l'affichage. Ne pas réintroduire de lecture de devise « au hasard » (l'ancien `limit(1)` côté Clients a été retiré).
- **Migrations: write the file and stop. Schema to prod before code.** (See Engineering rules 2 & 3.)
- Every new RLS `UPDATE` policy includes a `WITH CHECK`. New columns start nullable.
- The client never decides a transition. `performTransition`/`transition_order` validate preconditions and return `allowedActions`; the UI only renders what the server returns.
- `orders.items_summary` (jsonb) holds line items; since Phase 3a it also captures Shopify `product/variant/sku` ids per line (older orders have free-text titles only → resolved best-effort). `orders.source` distinguishes origin.

## Phase tracker (update at the end of every phase)

| Phase | Scope | Migrations | Status |
|---|---|---|---|
| **0** | Server transition engine + confirm/schedule bug fix + critical-path E2E | 0020 | ✅ Done |
| **1** | 4-dimension model + backfill + dual-write trigger + extended audit log | 0021–0024 | ✅ Done (zero reconciliation drift, RLS green) |
| **2** | Unified list + 8 saved views + inline status + manual creation + search + remove "Frais & taxes" | 0025–0026 | ✅ Done |
| **3a** | Product catalogue (`product` table, `read_products` scope, capture Shopify line ids, product selector) | 0027 | ✅ Done (catalogue populated in prod) |
| **3b** | Stock module: `stock_movement` + `product_stock` + `order_line` + `post_stock_movement` (atomic) + movements in `transition_order` + CUMP + thresholds + manual adjustment + courier_return + Stock page + reconciliation filet | 0028–0030 | ✅ Done |
| **4** | Drivers: `stock_movement.driver_id` + `allocate_to_courier`/`courier_return_lot` (stock en main **dérivé du ledger**, lot + per-order) + cash consolidation (reuses cash tables) + performance + Livreurs tab | 0031 | ✅ Done (RLS green incl. invariant, E2E green) |
| **5** | Purchases: supplier lots + business-day ETA + landed cost → CUMP | 0033–0034 | ✅ Done (0034 en prod, types régénérés, cast `GenericRpc` retiré, RLS 36/36 verts, **E2E `purchases.spec.ts` 6/6 verts sur chromium/pixel-7/iphone-14**). |
| **6** | Finance: returns-aware revenue + COGS + expenses + net profit + owner-only | 0035 | ✅ Done (RLS 47/47 verts, E2E 12/12 verts sur chromium/pixel-7/iphone-14). **Note: `merchant_levy_bps` exclus du calcul — nature non documentée, à clarifier avec expert-comptable OHADA avant usage fiscal.** |
| **6b** | Produits — pagination serveur (lot 25, **OFFSET pur** via `.range()` PostgREST — pas de seek keyset), recherche ILIKE titre/SKU, onglet Achats (`?tab=achats`, owner-only) | 0036 | ✅ Done (E2E 15/15 verts sur chromium/pixel-7/iphone-14). **Note : 0036 sert le tri stable A-Z `(merchant_account_id, lower(title), id)` et le filtre tenant ; la pagination OFFSET est acceptable à <500 produits. Pas de doublon/saut en nominal ; possible si insert concurrent entre deux pages.** |
| **6c** | Finance finition — **fix COGS=0/marge 100%** (fallback CUMP + exclusion des lignes au coût inconnu), cards Encaissé/À encaisser séparées + Résultat net + Marge brute clarifiée, graphes finance **période-aware** + CA encaissé par boutique, **export PDF P&L complet** (compte de résultat + charges + marge par produit, owner-only) tout en gardant le CSV SYSCOHADA | — (aucune) | ✅ Done (unit verts dont régressions COGS, RLS 47/47, **E2E `finances.spec.ts` 18/18 = 6×3 cibles**). **COGS** : coût figé prioritaire ; figé=0+CUMP → estimé (badge « marge estimée ») ; figé=0 ET CUMP=0 → ligne **exclue + signalée** (pas de 0 silencieux), comme les commandes sans ligne. Le badge « estimée » est un signal de qualité de données **permanent** (produits jamais costés via lot Phase 5 / lignes non résolues), pas un résidu de test. Requête diagnostic : `supabase/snippets/diagnostic_cogs_quality.sql`. **Graphes** : nouvelle action owner-only `getFinanceChartsAction` (les actions dashboard `getRevenue30d/getCodBreakdown/getShopPerformance` restent intactes pour `/tableau`). **PDF** : builder partagé `lib/finance/report-data.ts` (`fetchFinanceReport`), P&L `null` pour un manager. |
| **7a** | Shopify — fondation sync : multi-boutiques, tokens offline expirants chiffrés + refresh, webhooks idempotents + miroir de canal, bulk deux vitesses + réconciliation nocturne, GDPR réels, désinstallation par boutique | 0037 | ✅ Done (unit verts dont idempotence/garde hors-ordre/parser bulk, RLS 47/47, E2E webhooks skip propre en local — split `.env.local`/`.env.test` du secret HMAC, s'exécute en CI propre). **Décisions** : on a fait ÉVOLUER `shop` (pas de table `shopify_shop`) et ÉTENDU `webhook_event` (pas de `processed_webhooks`). **Shopify n'écrit JAMAIS les 4 dimensions** : statuts dans colonnes miroir `shopify_*` ; un `fulfilled` ne marque pas LIVREE. **Idempotence** : dédup par `X-Shopify-Webhook-Id` + upserts par clé naturelle `(shop_id, shopify_order_id)`. **200 rapide** + traitement en `after()`. **Garde hors-ordre** via `shopify_updated_at` (`isStaleShopifyUpdate`). **Réconciliation** : `cron/shopify-reconcile` (02:00) + fallback polling bulk. Checklist publication : `docs/shopify-publication-checklist.md`. **Restes (7b/7c)** : backoff THROTTLED bulk, dérivation fine `refunds/create`. |
| **7b** | Shopify — enrichissement client : import PII enrichie (nom/prénoms, téléphone E.164, email, adresse flexible jsonb, tags, consentement, numberOfOrders/amountSpent), dédup robuste téléphone→email→GID à travers boutiques ET canaux, détection récurrents/refuseurs, GDPR réel sur la PII | 0038 | ✅ Done (unit 327 dont normalisation/dédup/fusion/adresse, RLS 48/48 incl. isolation customer enrichi, E2E `clients.spec.ts` 3/3 chromium/pixel-7/iphone-14 ; régression navigation onglet Achats corrigée, `purchases.spec.ts` 6/6 verts sur chromium/pixel-7/iphone-14). **Décisions** : `customer` ÉTENDU (pas de nouvelle table), reste CROSS-boutiques (pas de `shop_id`). **AUCUN compteur stocké** : récurrence (`order_count>1`) + refus (`refused_count`, seuil 2) restent **dérivés à la volée** par la RPC `0014` (cod_status='REFUSEE') — surfacés en TS, pas de double source. Dédup unique `(merchant_account_id, phone_e164)` ; helper SQL `sn_phone_e164` (port de `normalizeSenegalPhone`, garde les fixes 33) ; validation mobile `7x` SÉPARÉE (`isSenegalMobile`). 3 adaptateurs (GraphQL incrémental + bulk JSONL + webhook REST) alignés sur la même forme `ShopifyCustomerNode`, normalisée dans `mapShopifyCustomer`. **GDPR réel** : `customers/redact` efface TOUTE la PII (retrouvé par GID legacy **et** tableau `shopify_customer_gids`) ; `shop/redact` n'anonymise que les clients EXCLUSIFS à la boutique ; `data_request` compile tout. Checklist publication : champs données protégées niveau 1+2 documentés. **Restes (7c)** : analytics annulations/retours ; (dette 7a) backoff THROTTLED bulk, dérivation fine `refunds/create`. |
| **7c** | Shopify — analytics annulations/retours | 0039 | ✅ Done (analytics owner+manager sur `/analyses` : scorecard canal, tendance RTO, pertes par produit/zone/livreur, refuseurs répétés ; calculs depuis `orders` + `audit_log` avec séparation stricte **RTO=failed** vs **retour=returned après delivered** ; **fallback état courant** (`isCurrentCancellation/Return/Rto`) si l'`audit_log` manque — `returned` compté seulement si `returned_at` présent (pas de double comptage) ; dette 7a soldée avec backoff exponentiel `THROTTLED` bulk et dérivation fine `refunds/create` basée sur les transactions réussies non-COD ; unit 342/342, RLS 48/48, **E2E `analytics.spec.ts` 9/9 sur chromium/pixel-7/iphone-14**). |
| **8** | Assistant IA (function-calling lecture seule, RLS-scoped) + FAQ : catalogue fixe d'outils, 2 couches RBAC, prompt durci, audit, rate-limit, onglet IA/FAQ | 0040–0042 | ✅ Done (unit dont red-team injection/fuite de rôle/anti-fabrication, RLS non-skipped incl. les 2 RPC finance owner/manager, **E2E `assistant.spec.ts` 4/4 sur chromium/pixel-7/iphone-14**). **Décisions** : Vercel AI SDK v6 + Groq `llama-3.3-70b-versatile` (température 0, `maxRetries` 3 = backoff 429) ; **pas d'auth hook** (rôle via `current_member_role`) ; **aucune lecture service-role** — le coût (`unit_cost`, hors-grant) passe par les RPC `SECURITY DEFINER` role-gardées `ia_finance_cost_movements`/`ia_product_cump` (garde NULL-safe : `v_role is null or v_role not in (...)`, cf. correctif `0042`), le reste réutilise la fonction pure `computeFinanceReport`. **2 couches RBAC** : couche A = `getToolsForRole` (l'outil n'existe pas pour le rôle) ; couche B = `runTool` (re-check rôle + validation Zod/enums + audit `ia_tool_audit`). Marge/profit = owner only ; CA/COGS + RTO/annulation/perf livreur = owner+manager ; statuts/stock/top produits (unités)/fiabilité = tous. **`ia_tool_audit`** ENABLE-sans-FORCE (écriture via definer pour journaliser les refus, SELECT owner-only). **Restes (Phase 9)** : actions d'écriture (après revue sécurité dédiée), pgvector/RAG si la FAQ grossit, confirmer la clause « no training » des ToS Groq avant prod. |
| **9** | Finition durcissement (sécurité P1 + reliquat perf + vérif). **Stage 0** audit + **Stage 1 P0** : keyset pagination commandes (`(sort_at, id)`, colonnes STORED), 19 DEFINER NULL-safe, CVE-2025-29927. **Finition** : CSP deux régimes + en-têtes (middleware header-only), GitLeaks CI, log A09 (Sentry), e2e prod-build (Router Cache), sweep tenant-isolation, **D.2 perf TTFB `(app)`** (streaming Suspense `/tableau` + collapse cascade layout ~6→2 RTT). | 0044–0045 (Stage 1) ; **aucune en finition** | ✅ Done (CSP/GitLeaks/A09/C.1/D.1/D.2 — PR #8 ; voir « Durcissements Phase 9 »). **A.3 rate-limiting Upstash = STOP documenté** (env `UPSTASH_REDIS_REST_URL/TOKEN` pas posées sur Vercel). **B.1 recharts = déjà fait** (mesuré : chunk async 348K, absent du first-load ; pas sur `/commandes/[id]` ni `/clients`). **Finding hors-périmètre** : `merchant_member.INSERT` scopé `is_member_of` (tout membre), pas owner-only — garde « non-owner ne crée pas de membre » seulement applicative (`requireRole('owner')`), pas RLS → à corriger par une future migration. |
| **10 — min. PII** | Retrait de l'email du **client final** (décision PCD : Tëër ne demande plus l'accès à l'email client Shopify, non utilisé en COD → minimisation). Code : arrêt sync (requêtes GraphQL/bulk + webhook REST, mappers, **dédup secondaire par email retirée**, merge patch), création manuelle (`findOrCreateCustomerByPhone`/`getOrderById`), GDPR (compile/anonymize), DTO `CustomerListItem.email`. Migration `0049` : recrée `get_/list_customer_reliability` **sans** le champ email (DROP+CREATE, signature `RETURNS TABLE`) puis `DROP COLUMN customer.email` (le DROP **purge** les valeurs). Docs légaux **v1.2** : « adresse électronique » retirée des données **Clients finaux** (`politique-confidentialite` art. 4.b/5 + `dpa` art. 2), re-seed `legal_documents` (`0050`) → re-prompt Stage 4. | 0049–0050 | ✅ Done (typecheck/lint, unit 378, RLS 57/57 non-skip, **CI 5 jobs verts** PR #7 ; référent E2E manqué `clients.spec.ts` corrigé). **Conservé** : email du **Marchand** (compte) + `accepts_marketing` (consentement, pas une adresse). `cgu`/`mentions` restent en 1.1 ; hashes v1.2 privacy `ec5cdd4b…`, dpa `502f1573…`. |

**Migration `0057` — fix KPI dimensionnel + intégrité dispatch (PR dédiée) :** corrige 3 bugs de la passe manuelle (#5a/#5b/#6), tous dus au fait que `get_dashboard_kpi` (RPC `0010`) était resté sur le legacy `cod_status` + `created_at`. **(a)** Réécriture `get_dashboard_kpi` sur le **modèle dimensionnel** : `ca_collecte_7j` = `SUM(total_amount)` sur **`cash_collected_at >= now()-7j`** (aligné Finances, libellé honnête « CA collecté (7 j) ») ; `ca_en_attente` = `SUM(coalesce(cash_collectable_minor, round(total_amount)))` où **`cash_state='expected'`** (aligné Livreurs « attendu », pour que les deux chiffres coïncident) ; sparkline sur `cash_collected_at`. Mêmes clés jsonb → `toDashboardKpi` inchangé. **(b)** Invariante **dispatch ⇒ livreur** : réparation des commandes `assigned`/`out_for_delivery` orphelines → `scheduled` (AVANT la contrainte), puis CHECK `orders_dispatch_requires_driver` (NULL-safe, scope `assigned`/`out_for_delivery`, exclut `delivered`/`failed`/`returned` qui conservent le livreur pour l'attribution cash). Trou fermé : les chemins inline (`updateCodStatusAction`/`transitionOrderStatusAction`/Kanban mobile) appelaient `assigner` sans livreur. **Garde moteur** `missing_driver_for_dispatch` dans `performTransitionForContext` (refus avant mutation + message FR) = défense en profondeur au-dessus du CHECK. **Bug latent séparé noté (NON corrigé) :** `a_appeler_delta` n'est pas un vrai snapshot J‑1 (il compte les `A_APPELER` *actuelles* créées il y a > 1 j, pas l'état d'hier) → recopié verbatim de `0010`, à traiter dans un lot KPI ultérieur. RLS 70/70, unit 397, e2e verts (chromium/pixel-7/iphone-14).

**Dette technique à solder — migration `0054` (drop différé Lot C) :** la migration `0053` (Lot C) a AJOUTÉ + BACKFILLÉ `purchase_lot.transport_total`, `purchase_lot.estimated_lead_time_days`, `purchase_lot_line.purchase_price_total` et rendu `purchase_lot_line.unit_purchase_price` nullable, **sans rien dropper**, pour ne pas casser le build entre le push schéma et la réécriture du front. Le front (Étape 2/3 Lot C) ne lit plus les anciennes colonnes. **Reste à faire (pas optionnel, juste différé) après merge de ce front : une migration `0054` qui DROP** `purchase_lot.freight_total/customs_total/transit_total/local_transport_total`, `purchase_lot.supplier_prep_days/transport_days/local_buffer_days`, `purchase_lot.eta_override` (+ ajuste les grants). `unit_purchase_price` est **conservé durablement** (affichage historique « prix d'achat » des lots reçus avant Lot C) ; `shipping_mode`/`allocation_method` restent en place (hors périmètre). Le coût atterri/CUMP des lots déjà reçus n'est pas impacté (valeurs figées par ligne).

**Durcissements Phase 9 (finition) :**
- **CSP deux régimes** (`middleware.ts` header-only + `lib/security/csp.ts`) : (a) **app dynamique** → `script-src 'self' 'nonce-{N}' 'strict-dynamic' https:` (sans `unsafe-inline`), `connect-src` dérive les hôtes PostHog (ingest+assets), Sentry par tunnel same-origin `/monitoring` ; (b) **pages publiques statiques** (`/`, `/confidentialite`, `/conditions`, `/dpa`, `/mentions-legales`, `/connexion`) → `script-src 'self' 'unsafe-inline'`. *Décision* : pas de hash sur le statique — `experimental.sri` ne signe QUE les scripts externes (testé : les ~33 scripts inline flight RSC restent), et un pipeline de hash n'est pas livrable (hash connus après build / middleware bundlé pendant). `unsafe-inline` confiné aux pages sans auth/input/secret ; ne touche jamais l'app. `dev` ajoute `'unsafe-eval'` + `ws:` (HMR). **`upgrade-insecure-requests` en prod (https) UNIQUEMENT** : WebKit (iPhone) l'applique jusqu'à `http://localhost` → page blanche → tous les E2E iphone-14 en timeout (Chromium exempte le loopback) ; inutile en local http, redondant avec HSTS preload en prod. Pages publiques restent `○` (cache + Lighthouse préservés).
- **En-têtes communs** (`next.config.mjs` `headers()` sur `/(.*)`) : HSTS `max-age=63072000; includeSubDomains; preload`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=(), browsing-topics=()`. Strip `x-middleware-subrequest` (défense CVE-2025-29927 ; vraie protection = Next 15.5.x).
- **GitLeaks CI** (`.github/workflows/ci.yml` job `gitleaks`, `.gitleaks.toml`) : scan d'historique parallèle de lint, échec sur fuite ; allowlist lockfile/.env.example. Aucun secret en `NEXT_PUBLIC_`.
- **Log A09** (`lib/security/authz-audit.ts`) : `requireRole` capture un event Sentry `warning` (`security_event:authorization_failure`) avant FORBIDDEN. Pas d'`audit_log` (schéma transitions inadapté, et aucune migration en Phase 9).
- **Rate-limiting Upstash (A.3)** : **NON FAIT — STOP documenté.** Prérequis env Vercel (`UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`) pas posés. Le limiter assistant existant (`lib/ia/rate-limit.ts`, par-utilisateur côté Postgres) reste en place.
- **recharts (B.1)** : déjà en `next/dynamic` + `ssr:false` + skeleton sur `/tableau`, `/finances`, `/analyses`. **Mesuré** : recharts (chunk `846.js` ≈ 348K) en async, **absent** des chunks first-load (framework/main-app/webpack). First Load JS inchangé (partagé 104 kB ; `/commandes` 139 kB, `/commandes/[id]` 198 kB, `/clients` 181 kB, `/tableau` 179 kB, `/analyses` 109 kB). Middleware bundle 91.9 kB (edge, hors client). Aucune optimisation spéculative ajoutée.
- **e2e prod-build (C.1)** : option (a) — workflow `e2e-prod.yml` (`next build`+`next start`) exécutant les specs gardées `E2E_PROD_BUILD=1` (issue #3 Router Cache + pagination keyset), sur `workflow_dispatch` + PR touchant nav/cache. `playwright.config` sert `pnpm start` si `E2E_PROD_BUILD=1` (dev inchangé). Cibles **chromium + pixel-7** uniquement (moteur Chromium ; WebKit casserait sur `http://localhost` à cause de l'UIR du build prod — couverture WebKit assurée par `test-e2e` dev).
- **Tenant-isolation (D.1)** : `tests/rls/tenant-isolation.rls.test.ts` — sweep data-driven (RLS FORCE + policies tenant-scopées/déni + WITH CHECK en écriture sur chaque table tenant ; exception documentée `ia_tool_audit`) + role-escalation RLS (purchase_lot owner-only) + XOF scale-0 + déni cross-tenant. Non-skip en CI (dép `pg`). 61/61 RLS verts en local.
- **Re-mesure p95 / Lighthouse (D.2)** : gain keyset Stage 1 (399 ms → 0,4 ms) structurellement intact (aucune requête liste touchée en finition). **Lighthouse mobile + p95 charge non re-mesurés dans l'env de travail** (ni Docker ni CLI lighthouse) — à relancer via la preview Vercel / l'outillage du dev ; le régime CSP statique préserve par conception le cache et le score Lighthouse de la landing. **Perf TTFB `(app)`** : (a) **streaming Suspense `/tableau`** (shell streamé + suspense par bloc, fix LCP/TTFB) ; (b) **collapse de la cascade de requêtes du layout `(app)`** — plancher ~6→2 RTT wall-clock (`getUser` → `Promise.all(consents ∥ member+role ∥ messages)` → `merchant_account` conditionnel ; lectures consents parallélisées, `getUser`/`merchant_member` redondants supprimés). **Gain structurel** (round-trips éliminés) ; **mesure terrain à confirmer sur CrUX / preview EU** — non chiffrable depuis Dakar à cause du bruit réseau Dak→fra1.

**Transition → movement mapping (authoritative SQL, `0029`):** call→validated (delivery=unassigned) → `reserve` (+qty_reserved, soft) · delivery→assigned/out_for_delivery → `dispatch` (−qty_on_hand, −qty_reserved, CUMP snapshot) · delivery→delivered → `sold` (COGS snapshot) · cancel/refuse when delivery∈{unassigned,scheduled} → `release` (−qty_reserved) · cancel/refuse/fail when delivery∈{assigned,out_for_delivery} → **no movement** (stock with courier; `courier_return` posted manually at physical return).

**Stock atomicity:** `transition_order` (0029) calls `post_stock_movement` per resolved `order_line` **within its own transaction**. An exception in `post_stock_movement` rolls back the entire transition. Unresolved lines (match_status ≠ 'matched') are silently skipped. Autonomous stock actions (`purchaseInAction`, `manualAdjustmentAction`, `courierReturnAction`) call `post_stock_movement` via `supabase.rpc()` — single atomic HTTP call. Nightly pg_cron (`0030`) runs `reconcile_product_stock()` and persists discrepancies in `stock_reconciliation_alert`; `rebuild_product_stock()` reconstructs from the ledger on demand.

**Driver stock (Phase 4, `0031`):** `stock_movement.driver_id` (nullable) attributes a movement to a `driver`. `post_stock_movement` accepts `p_driver_id` and two lot movement types: `allocate_to_courier` (advance lot leaving the warehouse to a courier, `−qty_on_hand`, CUMP snapshot, no order) and `courier_return_lot` (unsold lot back, `+qty_on_hand`); both require a driver (CHECK). `transition_order` now passes the order's effective driver to every per-order movement. **Driver stock-in-hand is DERIVED from the ledger** (`lib/drivers/stock-on-hand.ts`: `Σ −qty` over `dispatch/allocate_to_courier/sold/courier_return/courier_return_lot` for the driver) — never a separate source of truth; invariant `Σ driver-in-hand + warehouse = ledger` holds. Cash per driver **reuses** `cash_settlement`/`settlement_allocation`/`settlement_shortfall` (`record_cash_settlement` global remittance) — `lib/drivers/cash-consolidation.ts` derives dû/collecté/remis/écart. Livreurs tab (`/livreurs`, owner/manager) shows stock-in-hand, cash, performance per driver. Autonomous lot actions: `allocateToCourierAction`, `courierReturnLotAction`.

**Purchases / landed cost (Phase 5, `0033`–`0034`):** a supplier shipment is a `purchase_lot` (header: supplier, ref, `ordered_at`, `shipping_mode`, `supplier_prep_days/transport_days/local_buffer_days`, `eta_override`, four shared-fee columns `freight/customs/transit/local_transport_total`, `allocation_method`, `status ordered→in_transit→received`) + `purchase_lot_line` rows (`product_id`, `qty`, `unit_purchase_price`, and receipt-derived `line_value/allocated_fees/landed_total_value/landed_unit_cost`, nullable until received). **Shared fees are spread across lines by the largest-remainder method, weighted by line value, in pure bigint** (`lib/purchases/fee-allocation.ts`: `Σ allocated = Σ fees` exactly; V=0 → equal split). **ETA = business days** skipping weekends (`lib/purchases/eta.ts`: `addBusinessDays`/`computeEta`; `eta_override` wins; Senegalese holidays handled via the manual override). **Receipt is atomic**: `receive_purchase_lot(p_lot_id, p_merchant_account_id, p_actor_id, p_lines jsonb)` (`0034`, SECURITY DEFINER) locks the lot, writes the derived columns per line, and posts one `purchase_in` per line via `post_stock_movement` **passing `p_received_value = landed_total_value`** so the CUMP numerator uses the exact landed total (no rounding drift from `qty × floor(landed/qty)`) — all in one Postgres transaction. `post_stock_movement` gained `p_received_value bigint default null` in `0033` (backward-compatible; **only the `purchase_in` branch changed** — every other movement type is byte-for-byte the 0031 body). **Owner-only**: `purchase_lot`/`purchase_lot_line` RLS FORCE restricts SELECT/INSERT/UPDATE to `current_member_role = 'owner'`; manager/agent see zero rows and cannot write (proven in `tests/rls/purchases.rls.test.ts`). Actions in `lib/actions/purchases.ts` (`createPurchaseLotAction`, `updatePurchaseLotAction`, `add/removePurchaseLotLineAction`, `markLotInTransitAction`, `receiveLotAction`, `getPurchaseLotPageData`) all use `requireRole('owner')`. UI: owner-only "Achats fournisseur" **onglet** (`/produits?tab=achats`) via `components/purchases/purchase-lots-view.tsx` — shows the per-line landed-cost breakdown + a `Σ frais alloués = Total frais` reconciliation line; `merchant_settings.import_vat_recoverable` (bool, default true) is reserved for Phase 6 VAT handling. **Display-rounding note (verified in prod, bounded, NOT a bug):** the Stock page value is `qty_on_hand × unit_cost` recomputed live (`lib/actions/stock.ts:283`; `product_stock` has no stored value column — `total_value` deliberately deferred at Stage 0). `unit_cost` is the CUMP rounded to integer FCFA (`(numerator/qty)::bigint`, round-to-nearest), so the displayed value can differ from a line's exact `landed_total_value` by ≤ `qty_on_hand × 0.5 F` (e.g. Electric lot: exact 43 957 vs displayed 43 960 = 40 × round(1098.925)=1099 → +3 F). Because value is a pure function of the current `(qty_on_hand, unit_cost)` with no accumulating stored total, and outflows carry drift away proportionally, this does **not** accumulate across lots (zero-mean, √n at worst). Plan B if ever needed for accounting: store `total_value numeric` on `product_stock` and derive `unit_cost` for display from it.

**Finance / P&L (Phase 6, `0035`):** two new timestamptz on `orders`: `cash_collected_at` (set when `delivery=delivered AND cash=collected`) and `returned_at` (set when `order_state` transitions to `returned`) — both written atomically in `transition_order` (only the SET clause changed; full function body preserved). `expense_category` + `expense` tables: FORCE RLS **owner-only** (manager/agent see zero rows); 5 system categories auto-seeded per tenant via `merchant_account_seed_expense_categories` trigger. **CA = only orders with `cash_collected_at` in the period** (not `cod_status=LIVREE`). Retours: contra-revenue only for orders with `cash_collected_at`. COGS reversal at frozen `unit_cost` for restocked `courier_return` movements. Mobile money fees: `round(amount × feeBps / 10_000) + min(round(amount × transferTaxBps / 10_000), cap)` — BigInt arithmetic, no float drift. **`merchant_levy_bps` excluded from profit net — nature non documentée, à clarifier.** Navigation: finances is now **owner-only** in sidebar + bottom-tab-nav. Disclaimer banner permanent on the page. CSV export mapped to SYSCOHADA accounts (706/6031/627/category-account). Pure computation in `lib/finance/profit.ts`; action in `lib/actions/profit.ts` + `lib/actions/expenses.ts` (all `requireRole('owner')`); UI in `components/finance/ProfitSection.tsx` + `components/finance/ExpenseSection.tsx`.

| **11** | Frais de livraison + workflow commande + réassignation livreur | 0058–0060 | ✅ Done (workflow « Programmer »/« Assigner », édition montants + frais de livraison, réassignation avec compensation stock, card CA net livraison, page Livreurs cash net, **unit/lint/typecheck/build/RLS verts** et **E2E Phase 11 verts sur chromium/pixel-7/iphone-14**). |
| **11.1** | Finition workflow page commande : étape « Assignée » distincte + popup d'assignation (détails/prix) → passage en livraison | 0061–0062 | ✅ Done (action `demarrer_livraison`, statut d'affichage « Assignée » vs « En cours de livraison », **popup détails/prix séparé après le choix du livreur** (liste ET détail), `assigned` reste dans « Programmer » jusqu'à confirmation, « Démarrer la livraison » retirée des dropdowns ; unit 411 / lint / typecheck / build / RLS 70/70 verts ; **E2E à valider en CI**). |
| **12** | Finances 3 vues + coût de revient pilotage + COGS par livreur | 0063 | ⏳ Code complet + CI 3 cibles verte — **vérif prod en attente** (Finances `?tab=global|produits|livreurs`, CA unifié « Chiffre d'affaires », coût de revient complet pilotage par produit, COGS figé par livreur). **Fix PDF** : `kpis.caLivre` (renommée `kpis.caUnified` au Stage 5) référencée par le rapport → `MISSING_MESSAGE`, corrigé. **Fix crash `BigInt(float)`** : les vues par produit/livreur levaient un 500 en prod (`2536.06 cannot be converted to a BigInt`) — `items_summary.price` (jsonb décimal) et `orders.total_amount` (`numeric`) passés à `BigInt()` ; helper `toMinor()=BigInt(Math.round(x))` sur toute valeur monétaire lue + tests unit (prix/total fractionnaires, divisions non exactes) + E2E rendant réellement les 2 onglets sur données fractionnaires. **Mode lot d'avance RÉTABLI** (revert du retrait Stage 2, TS-only) : `allocate/courier_return_lot`, `DriverLotForm`, dérivation stock-en-main + invariant RLS lot des deux côtés restaurés, tout en conservant l'historique versements + cash total (Stage 3/4). unit 416 / lint / typecheck / build / RLS 70 verts ; **CI E2E 3 cibles (chromium/pixel-7/iphone-14) verte** (run 27722821699). **Restes** : (a) marge ≠ `CA − coût_revient(B)` car marge sur COGS officiel (A) et B sur coût atterri reçu (Méthode A) ; (b) bandeau d'approximation §2 non explicite — à trancher avec le marchand. |

| **12.1** | Finition Finances — vue par produit juste, marges cohérentes, libellés simples, filtres, mobile, édition inline | — (aucune) | ✅ Done (code + sanity verte ; **vérif prod en attente**). Aucune migration (calcul + UI). unit 421, lint/typecheck/build verts. RLS inchangé (aucune surface SQL/RLS touchée — `updateProductUnitCostAction` owner/manager préexistante) → tourne en CI. E2E 3 cibles à valider en CI (specs ajoutées : presets rechargent, vue globale sans versements, coût manquant éditable + card à définition). **Voir « Finition Phase 12.1 » ci-dessous.** |

**Finition Phase 12.1 (S1–S8) :**
- **S1/S2 — justesse (fix de fond) :** la composante coût d'achat du coût total (B) = **CUMP figé** des mouvements `sold` (`officialCogs`, base A), PLUS « lots reçus dans P ÷ qté vendue » qui faisait exploser le coût à faible volume (IAS 2 §13). Chargement `purchase_lot`/`landed` **supprimé** (mort) + boucle O(lignes×lots) éliminée. **Bénéfices appariés (a1)** : `bénéfice avant = CA − prix d'achat (A)` ; `bénéfice après = CA − coût total (B = A + pub + livraison)` — invariant structurel (impossible d'afficher un bénéfice positif si le coût dépasse le CA). **Garde-fou faible volume** : `lowVolume` (qtySold < `LOW_VOLUME_THRESHOLD=3`) → l'UI montre les **totaux période** + badge, jamais d'unitaire absurde ; les totaux restent CA − coût (cohérents, pas unitaire×qté). **Coût manquant** : badge sur `unit_cost IS NULL` ET COGS figé=0 (jamais sur 0 assumé/cadeau) ; bénéfices dépendants en « — ».
- **S3 — libellés simples + cards à définition :** composant partagé `components/ui/definition-card.tsx` (`DefinitionCard` valeur autonome + `DefinitionToggle` à embarquer), explication langage marchand **au tap** (pas survol). Appliqué à Finances Global (Marge brute, Résultat net, RTO), vue Produit (header), et Tableau (2 cards « CA » via toggle dans `KPICard` — sparkline/delta préservés — + « Répartition COD »). Livreurs exclus (déjà clairs). Périmètre figé avec le marchand (minimal).
- **S4 — filtres période :** fix du bug — les presets (Aujourd'hui/7j/30j/**90j ajouté**) ne portent plus `from`/`to` (sinon la branche `custom` gagnait dans `periodRange` → clic ignoré) ; `periodLinkParams` transporte la bonne sémantique au changement d'onglet. Skeleton via `loading.tsx` ; `error.tsx` (Réessayer + Sentry) ; persistance localStorage (`FinancePeriodPersistence`, restore borné à l'absence de période → pas de boucle).
- **S5 — perf + §3.6 :** **streaming Suspense** (coquille immédiate + `FinanceTabSkeleton` par onglet, `key={tab-period}`) ; **« Versements par livreur » retiré de la Vue globale** (doublon) et relocalisé dans l'onglet Livreurs (la page `/livreurs` le conserve aussi) ; requête `buildDriverSettlements` de la globale (morte) supprimée. Cause de lenteur documentée : **pas de N+1 DB** ; latence = rendu bloquant (await inline) sans streaming + re-filtrage TS (corrigé par indexation Map des lignes par order_id). Aucun index/migration requis.
- **S6 — édition in-cell :** cellule « Prix d'achat » éditable (pattern du seuil par produit) → `updateProductUnitCostAction` (owner/manager, action produit existante, **pas de mouvement stock fabriqué**). **Repli estimé** dans le compute pour rendre l'édition utile : COGS figé prioritaire ; à défaut `unit_cost × qté` (badge « estimée », précédent 6c) ; `unit_cost` null → coût manquant. Cellule éditable **uniquement** quand `unit_cost` pilote la valeur (manquant/estimé) — un COGS figé ne se réécrit pas ici (§2 lecture pure : `product_stock`/`unit_cost` officiel/COGS jamais réécrits). Mise à jour optimiste + totaux dérivés de l'état.
- **S7 — mobile :** tableau `hidden md:block` ; sur mobile une **card par produit** (`md:hidden`, en-tête nom + CA, primaire prix d'achat éditable + bénéfice avant, détails secondaires dépliables, card de totaux) — réutilise l'état/PurchaseCell/badges du desktop.
- **S8 :** unit (estimé/COGS-prime/invariant/faible volume/coût manquant/`toMinor`) ; E2E `finances.spec.ts` étendu (3 nouveaux specs) ; tracker. **Restes hérités de Phase 12 (non résolus, à trancher avec le marchand) :** (a) marge globale (P&L) reste sur COGS officiel (A) tandis que la vue produit montre coût total (B) — natures différentes, écart assumé ; (b) bandeau d'approximation §2 non explicite.

**Phase 11.1 — workflow Assignée → En cours de livraison (option C) :** `assigned` et `out_for_delivery` partagent toujours le `cod_status` legacy `EN_LIVRAISON` ; on les distingue **uniquement à l'affichage** via `delivery_state` (source de vérité) — badge/statut via `resolveCodDisplayStatus`/`codDisplayLabel` (`components/orders/cod-status-badge.tsx`). Nouvelle action **`demarrer_livraison`** (assigned→out_for_delivery) au catalogue + state machine (self-loop `EN_LIVRAISON→EN_LIVRAISON`), **exclue de `getTransitionActionForTarget`** (cible partagée avec `assigner`) **ET retirée de tous les dropdowns** via `visibleAllowedActions` — elle n'est déclenchée QUE par le popup. **AUCUN mouvement stock** sur cette transition : le `dispatch` est déjà posté à l'assignation et le garde SQL `0029/0031` exclut `assigned` du re-dispatch. **Flux (2 étapes, retour marchand)** : « Assigner » → dialog choix livreur (`assigner`, dispatch posté) → **popup détails/prix séparé** (`components/orders/assignment-details-dialog.tsx`) ouvert automatiquement après l'assignation, **depuis la LISTE comme depuis le DÉTAIL** (porté par `OrderActionsMenu` : ref armé sur la confirmation du dialog livreur + prop `autoOpenAssignment` à l'ouverture d'une commande déjà assignée). Le popup charge total/frais/date via l'action `getOrderAmountsForAssignmentAction` (owner/manager ; nécessaire car `OrderListItem` ne porte pas `delivery_fee_minor`), affiche les articles en lecture, et « Confirmer et démarrer la livraison » = `updateOrderAmountsAction` (si modifié) → `demarrer_livraison` → remonte le `TransitionResult` (liste mise à jour localement, pas de `router.refresh`) → close. **Annuler/Échap → la commande reste assignée**, récupérable dans « Programmer ». `OrderAmountsEditor` redevient un simple éditeur de montants (« Modifier », sans transition) pour la consultation/édition ultérieure. Owner/manager pour popup + édition (gating `canEditAmounts`). **Migrations `0061`+`0062`** sur `list_orders_paginated` (def. 0060) + `orders_view_counts` (def. 0052) : **Programmer** (`confirmee`) = `unassigned/scheduled/assigned` · **En cours de livraison** (`en-livraison`) = `out_for_delivery` seul → une commande assignée non confirmée reste dans « Programmer », n'apparaît dans « En cours de livraison » qu'après confirmation. `matchesOrderSavedView` (TS) aligné. Helpers date/heure partagés : `lib/format/datetime-input.ts`.

**Phase 12 — règle comptable A/B :** le coût d'achat unitaire CUMP officiel reste le COGS figé des mouvements `sold` et ne réécrit jamais `product_stock` dans cette phase. La vue produit ajoute uniquement un coût de revient complet de pilotage = coût atterri des lots reçus dans la période + publicité allouée par part de CA + livraison commande allouée par article ; le transport amont est déjà dans `purchase_lot_line.landed_total_value` via `receive_purchase_lot`, donc aucune livraison-stock n'est réajoutée.

## Agent alternation workflow (Codex CLI ↔ Claude Code)

- On session start: read this file, then the **current phase prompt** (provided by the developer) which carries the precise task. This file = durable context; the phase prompt = the task.
- Commit + push before switching agents (`git status` empty).
- At end of phase: update the Phase tracker (status + migrations + remaining) in the **same final commit**.

---
*This file supersedes any implicit understanding. If in doubt between this file and an ad-hoc instruction, ask the developer.*
